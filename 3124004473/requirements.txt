# ---------------------------------------------------------------------------
# 论文查重程序 —— 运行时依赖
# ---------------------------------------------------------------------------
# 本程序【不依赖任何第三方库】，只使用 Python 3 标准库：
#
#   argparse, collections, dataclasses, math, os, re, sys, time, unicodedata
#
# 因此这里没有任何需要安装的运行时依赖，直接运行即可：
#
#   python main.py <原文文件> <抄袭版论文文件> <答案文件>
#
# 之所以刻意做到"零第三方依赖"，是因为评测环境不一定能联网安装包；
# 少一个依赖就少一处"在我机器上能跑"的失败点。
#
# 运行环境要求：Python >= 3.9（代码中用到了 dict[str, int] 与
# dataclasses 的现代写法；已在 Python 3.12.4 上验证）。
#
# ---------------------------------------------------------------------------
# 开发期工具（可选，不参与评测运行，故全部注释掉）
# ---------------------------------------------------------------------------
# pytest>=7.0        # 单元测试运行器（tests/ 目录下 40+ 个用例）
# coverage>=7.0      # 测试分支覆盖率统计
# pylint>=2.16       # 代码质量分析（Code Quality Analysis）
# flake8>=7.0        # PEP 8 风格检查
# black>=24.0        # 代码自动格式化
